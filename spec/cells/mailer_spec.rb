require 'spec_helper'

describe Cells::Mailer do
  it 'has a version number' do
    expect(Cells::Mailer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
